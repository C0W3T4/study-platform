# Study Platform Mobile

## 🚀 How to run

- Clone the repository
- Install the dependencies with `npm i`
- Change **IP** in `baseURL` prop in `mobile\src\services\api.ts` file
- Start the development server with `npm start`
